<Window x:Class="RecipeManager.Presentation.RecipeEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RecipeManager.Presentation"
        mc:Ignorable="d"
        Title="Wijzig Recept" Height="500" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Naam:" Grid.Row="0"/>
        <TextBox x:Name="nameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>
        <TextBlock Text="Voorbereidingstijd:" Grid.Row="1"/>
        <TextBox x:Name="preparationTimeTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>
        <TextBlock Text="Moeilijkheid:" Grid.Row="2"/>
        <ComboBox x:Name="difficultyComboBox" Grid.Row="2" Grid.Column="1" Margin="5">
            <ComboBoxItem Content="Makkelijk"/>
            <ComboBoxItem Content="Medium"/>
            <ComboBoxItem Content="Moeilijk"/>
        </ComboBox>
        <GroupBox Header="Ingrediënten" Grid.Row="3" Grid.ColumnSpan="2" Margin="0,10,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox x:Name="ingredientsListBox" Margin="5"/>
                <Grid Grid.Row="1">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="5">
                        <TextBlock Text="Ingrediënt:" Margin="5"/>
                        <TextBox Name="ingredientTextBox" Width="200"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                        <Button x:Name="addIngredientButton" Content="Voeg ingrediënt toe" Margin="5" Click="addIngredientButton_Click"/>
                        <Button x:Name="removeIngredientButton" Content="Verwijder" Margin="5" Click="removeIngredientButton_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </GroupBox>
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="saveButton" Content="Sla op" Width="80" Margin="5" Click="saveButton_Click"/>
            <Button x:Name="cancelButton" Content="Annuleer" Width="80" Margin="5" Click="cancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>