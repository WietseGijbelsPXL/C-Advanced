<Window x:Class="Fruitshop.Presentation.OverviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fruitshop.Presentation"
        mc:Ignorable="d"
        Title="Overzicht fruit" Height="450" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="fruitListBox" Margin="20,20,20,10" />

        <Grid Margin="20,0,20,20" Grid.Row="1" Background="Black">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="createFruitButton" Grid.Column="0" Margin="5" Padding="10" Content="Create"/>
            <Button x:Name="deleteFruitButton" Grid.Column="1" Margin="5" Padding="10" Content="Delete"/>
        </Grid>
    </Grid>
</Window>
